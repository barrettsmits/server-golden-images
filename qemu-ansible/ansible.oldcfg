[defaults]
    roles_path = ../roles
    inventory     = hosts
    roles_path    = roles
    vault_password_file = ~/.ssh/ansible_vault_key
    host_key_checking = False
    remote_tmp     = /tmp/.ansible-${USER}/tmp

[collections]
    collections_paths = ~/.ansible/collections/

[inventory]
    enable_plugins = yaml, ini