---
# This is a sample file for the secrets.yml file.  It will not work as is.

# Proxmox API credentials
pve_iaac_password: your_password_here

# Cloud-init user credentials
ci_user_password_hash: your_password_hash_here

# SSH public key for cloud-init user
ssh_public_key: "ssh-rsa AAAAB............................................"

# Proxmox root user name
pve_root_name: root

# Proxmox root user password
pve_root_password: your_root_password_here